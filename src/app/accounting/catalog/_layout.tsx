import LayoutStack from "@/components/layoutStack";

const Layout = () => {
    return (
        <LayoutStack data={{
            index: { headerShown: false },
        }} />
    );
}
export default Layout;